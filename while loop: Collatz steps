# Read input
n = int(input())

steps = 0

# Apply rules until n becomes 1
while n != 1:
    if n % 2 == 0:   # even
        n = n // 2
    else:            # odd
        n = 3 * n + 1
    steps += 1

print(steps)
