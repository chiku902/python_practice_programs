# Read two lines of input
line1 = input().split()
line2 = input().split()

# Pair using zip and enumerate
result = {i: f"{a}->{b}" for i, (a, b) in enumerate(zip(line1, line2))}

# Print the result
print(result)
